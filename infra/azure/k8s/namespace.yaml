# Review sample. Terraform (kubernetes.tf) is the apply path. No env-specific
# Azure names here — safe to read; do not treat this directory as a hand-apply
# overlay for a later environment.
apiVersion: v1
kind: Namespace
metadata:
  name: torqvoice
  labels:
    app: torqvoice
    part-of: torqvoice
